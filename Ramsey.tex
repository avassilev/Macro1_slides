% !TeX spellcheck = bg_BG
\documentclass[leqno]{beamer}
\mode<presentation>
\usetheme{Warsaw}
\useoutertheme{split}
\usepackage{amsmath}
\usepackage{amstext}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsbsy}
\usepackage{booktabs}
\usepackage[round]{natbib}
\usepackage{rotating}
\usepackage{pstricks}
\usepackage[T2A]{fontenc}
\usepackage[cp1251]{inputenc}
\usepackage[bulgarian]{babel}
\usepackage[]{graphicx}

\newtheorem{thm}{Теорема}
\newtheorem{prop}{Твърдение}
\newtheorem{lem}{Лема}
\newtheorem{cor}{Следствие}
% \theoremstyle{remark} % наименованието е в италик (а не в курсив), тялото е стандартно (а не в италик)
\theoremstyle{definition} % наименованието е в курсив, тялото е стандартно (а не в италик)
\newtheorem{remark}{Забележка}
\newtheorem{defin}{Дефиниция}
%\numberwithin{thm}{section} \numberwithin{equation}{section}
%\numberwithin{figure}{section} \numberwithin{table}{section}
%%\renewcommand\theequation{\thesection.\arabic{equation}}
%%\def\rem{{\bf Забележка. }}
%\def\proof{{\bf \noindent Доказателство. }}
%\def\endprf {\mbox{$\blacksquare$}}
\newcommand{\beq}{\begin{equation}}
\newcommand{\eeq}{\end{equation}}
\newcommand{\bs}{\boldsymbol}
\DeclareMathOperator*{\argmax}{argmax}
\DeclareMathOperator*{\argmin}{argmin}
\DeclareMathOperator*{\sign}{sign}
\DeclareMathOperator*{\meas}{meas}
\DeclareMathOperator*{\essinf}{essinf}
\DeclareMathOperator*{\Ran}{Ran} \DeclareMathOperator*{\intr}{int}
\DeclareMathOperator*{\grad}{grad}
\DeclareMathOperator*{\dist}{dist}
\DeclareMathOperator*{\const}{const}
% \setbeamercovered{transparent}
\title[Модел на Рамзи]{Модел на Рамзи-Кас-Купманс}
\author{Андрей Василев}
\date{}


\begin{document}

\begin{frame}
\begin{titlepage}

\end{titlepage}
\end{frame}

\begin{frame}
\frametitle{Основни въпроси}
\framesubtitle{}
\begin{itemize}
	\item Уводни бележки
	\item Математически апарат
	\item Модел на Рамзи -- формулировка
	\item Изследване на модела на Рамзи
\end{itemize}
\end{frame}

\begin{frame}
	\frametitle{Уводни бележки}
	\framesubtitle{}
	\begin{itemize}\itemsep1em
		\item Основно допускане в модела на Солоу е това за екзогенна фиксирана норма на спестяване $s \in (0,1)$
		\item Подобно допускане може да бъде отслабено, доколкото е интуитивно, че при различни икономически обстоятелства се спестява различна част от дохода
		\item Моделът на Рамзи-Кас-Купманс (за краткост често е наричан само ,,модел на Рамзи'') позволява решенията за потребление, спестяване и съответно инвестиции да се вземат ендогенно, на основата на оптимално (в смисъла на решаване на подходяща оптимизационна задача) поведение на съответните икономически агенти, като по този начин обобщава модела на Солоу
		\item Отново разглеждаме случая на затворена икономика и работим в непрекъснато време 
	\end{itemize}
\end{frame}

\newcounter{slidenum} 
\setcounter{slidenum}{1}

\begin{frame}[fragile]
	\frametitle{Математически апарат (\arabic{slidenum})}
	\begin{itemize}\itemsep1em
		\item Постановките, които ще разглеждаме, изискват да се моделира оптимално поведение в динамика
		\item Един от възможните математически подходи за целта е с използването на апарат от теорията на оптималното управление
	\end{itemize}
\end{frame}

\begin{frame}[fragile]\stepcounter{slidenum}
	\frametitle{Математически апарат (\arabic{slidenum})}
	\framesubtitle{Постановка на задачата}
	Типична задача:
	\begin{equation}
		\max_{c(t)}\int_{0}^{T} f(k(t),c(t),t)\,dt,
		\label{eq:optctrlobj}
	\end{equation}
	при ограничението \textbf{фазовата променлива} $k(t)$ да удовлетворява уравнението
	\begin{equation}
		\dot{k}(t)=g(k(t),c(t),t),~0\leq t \leq T,
		\label{eq:optctrlstateeq}
	\end{equation}(където с точка над съответната буква е означена производната \`и по времето $ t $) при дадена начална стойност
	\begin{equation}
		k(0)=k_{0}>0,
		\label{eq:optctrlinitval}
	\end{equation}
	а $ c(t) $ се нарича \textbf{управляваща променлива} или \textbf{управление}.
\end{frame}


\begin{frame}[fragile]\stepcounter{slidenum}
	\frametitle{Математически апарат (\arabic{slidenum})}
	\framesubtitle{Постановка на задачата}
		\begin{itemize}\itemsep1em
		\item Променливите $ k(t) $ и $ c(t) $ са скаларни в разглеждания от нас случай
		\item За всички използвани по-нататък свойства на съответните обекти (напр. диференцируемост) ще предполагаме, че са изпълнени, без да правим изрични уточнения
		\item В типичните икономически приложения се приема, че $T=+\infty$ и съответно интегралът в целевия функционал \eqref{eq:optctrlobj} е от $0$ до $+\infty$ (това по принцип може да създаде математически трудности, които ние ще пренебрегнем тук)
	\end{itemize}
\end{frame}


\begin{frame}[fragile]\stepcounter{slidenum}
	\frametitle{Математически апарат (\arabic{slidenum})}
	\begin{itemize}\itemsep1em
		\item Тук ще се ограничим до алгоритмично получаване на необходими условия за оптималност
		\item За целта се съставя функцията на Хамилтън
		\begin{equation}
			J(k,c,t,\mu) = f(k,c,t) + \mu g(k,c,t),
			\label{eq:optctrlHam}
		\end{equation}
		където функцията $ \mu=\mu(t) $ се нарича \textbf{спрегната променлива}
		\item Необходимите условия в скаларния случай съдържат две уравнения:
		\begin{equation}
			\frac{\partial J}{\partial c}=0 \quad \text{ и }\quad \frac{\partial J}{\partial k} + \dot{\mu} = 0,
			\label{eq:optctrlNCs}
		\end{equation} и \textbf{условието за трансверсалност}
		\begin{equation}
			\lim_{t\rightarrow\infty} \mu(t) k(t) = 0.
			\label{eq:optctrltransvcond}
		\end{equation}
	\end{itemize}
\end{frame}



\begin{frame}[fragile]\stepcounter{slidenum}
	\frametitle{Математически апарат (\arabic{slidenum})}
	\begin{itemize}\itemsep1em
		\item Необходимите условия \eqref{eq:optctrlNCs} и \eqref{eq:optctrltransvcond} представляват конкретно приложение на т.нар. \textbf{принцип за максимума на Понтрягин}
		\item Първото условие от \eqref{eq:optctrlNCs} е частен случай на условие за максимизация на $J$ по $c$
		\item Условието за трансверсалност е необходимо условие само при специални случаи. Това е изпълнено при определени предположения за $ J $, $ \mu $ и \[ f(k,c,t) \equiv e^{-Rt}h(k,c,t),~R>0 \] (вж. края на \S 1.3.9 в приложението на Barro and Sala-i-Martin (1995))
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{}
	\begin{itemize}\itemsep1em
		\item В модела има домакинства и фирми
		\item Домакинствата съществуват вечно
		\item  Представителното домакинство се характеризира с определени активи $ A(t) $, потребление $ C(t) $ и трудово възнаграждение $ W(t) $
		\item Населението расте с постоянен темп $ n > 0 $:
		\begin{equation}
			L(t)=e^{nt},~L(0)=1.
			\label{eq:contramLgr}
		\end{equation}
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства}
	\begin{itemize}\itemsep1em
		\item Изменението на финансовите активи на домакинството от момента $ t $ до момента $ t+\Delta t $ има вида
		\begin{equation*}
			\begin{split}
				A(t+\Delta t)-A(t) = & \int_{t}^{t+\Delta t}r(\tau)A(\tau)\, d\tau +  \int_{t}^{t+\Delta t}W(\tau)\, d\tau - \\
				& \int_{t}^{t+\Delta t}C(\tau)\, d\tau ,
			\end{split}
		\end{equation*}
		където $ r(t) $ е лихвеният процент в дадения момент $ t $
		\item Като разделим на $ \Delta t $ и оставим $\Delta t $ да клони към нула, стигаме до диференциалното уравнение
		\begin{equation}
			\dot{A}(t)=r(t)A(t)+W(t)-C(t).
			\label{eq:contramA}
		\end{equation}
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства}
	\begin{itemize}\itemsep1em
		\item Домакинството може да има дълг, което се отразява в отрицателни активи: $A(t)<0$
		\item Предполагаме пълна заетост, т.е. броят на заетите е равен на населението
		\item Променливите на глава от населението  означаваме с малки букви: $ w(t):=\frac{W(t)}{L(t)} $, $ c(t):=\frac{C(t)}{L(t)} $ и $ a(t):=\frac{A(t)}{L(t)} $ 
		\item Тъй като 
		\[ \frac{\dot{A}(t)}{L(t)} = \left( \frac{A(t)}{L(t)} \right)^{.} + \frac{A(t)\dot{L}(t)}{[L(t)]^{2}} = \dot{a}(t)+n a(t), \]
		от \eqref{eq:contramA} следва
		\begin{equation}
			\dot{a}(t) = (r(t)-n)a(t) + w(t) - c(t)
			\label{eq:contrama}
		\end{equation}
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства}
	\begin{itemize}\itemsep1em
		\item Полезността от потреблението на един индивид се измерва с \textbf{функция на полезност} (наричана понякога и felicity function):
		\[ u=u(c),\quad u : [ 0,+\infty ) \longrightarrow \mathbb{R}, \]
		която е достатъчно гладка и има свойствата
		\begin{equation}
			\left\{ \begin{array}{l l}
				u'(c)>0, & u''(c)<0, \\
				\lim\limits_{c\rightarrow 0+}u'(c)=+\infty, & \lim\limits_{c\rightarrow +\infty}u'(c)=0.
			\end{array} \right.
			\label{eq:contramFelicity}
		\end{equation}
		\item Общата полезност в момента $ t $ е 
		\begin{equation}
			u(c(t))e^{nt},
			\label{eq:contramEconwideutil}
		\end{equation}
		т.е. индивидуалната полезност, умножена с броя на индивидите
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства}
	\begin{itemize}\itemsep1em
		\item За да се отчетат предпочитанията на потребителите по отношение на момента от времето, в който тази полезност се реализира, въвеждаме дисконтиращ множител $ e^{-\rho t},~\rho = \const > 0 $, за полезността \eqref{eq:contramEconwideutil}
		\item По-големите стойности на $\rho$ означават по-малка полезност за фиксиран момент от времето $t$, което може да се интерпретира като по-висока степен на ,,нетърпеливост''
		\item Ако полезността $ u(c(t)) $ е ограничена функция (например ако $ c(t)\in [0,C],~C = \const $), тогава при $ \rho>n $ изразът
		\begin{equation}
			U = \int_{0}^{\infty}u(c(t))e^{nt}e^{-\rho t}\,dt
			\label{eq:contramTotutil}
		\end{equation}
		за тоталната полезност за всички домакинства от избрания поток на потребление $c=c(t),~t\in[0,+\infty)$ е \emph{сходящ} интеграл.
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства}
	\begin{itemize}\itemsep1em
		\item Може да се покаже, че за оптималното потребление е невъзможно да се получи $c(t) = 0$ за някое $t$
		\item Потенциален проблем в модела е възможността представителното домакинство да натрупва верижно все по-големи дългове, с които едновременно да финансира потребление и да връща старите задължения. Това е т.нар. \textbf{игра на Понци} (Ponzi).
		\item За да забраним тази възможност ще поискаме асимптотично (т.е. за $ t\rightarrow \infty $) да бъде изпълнено активите на едно лице да бъдат неотрицателни
			\begin{itemize}
			\item В частност, това обхваща случая, когато отрицателните стойности на $ a(t) $ (т.е. дълговете за едно лице) клонят към нула
			\end{itemize}
	\end{itemize}
\end{frame}

\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства}
	\begin{itemize}\itemsep1em
		\item При нашите означения ограничението за забрана на игри на Понци се изразява в модифициран вид с условието
		\begin{equation}
			\lim\limits_{t\rightarrow +\infty} a(t)e^{-\int_{0}^{t}(r(\tau)-n)\,d\tau}\geq 0.
			\label{eq:contramNoPonzi}
		\end{equation}
		\item Модификации
			\begin{itemize}\itemsep1em
			\item Има дисконтиране
			\item Присъства множителят $ e^{nt} $ ( = брой на индивидите)
			\end{itemize}
		\item Интерпретация: настоящата стойност на нетните активи на агентите в цялата икономика асимптотично е неотрицателна
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства}
В крайна сметка се стига до следната задача: \bigskip

Да се максимизира по $ c(t) $ съвкупната полезност $ U $ от \eqref{eq:contramTotutil} при следните условия:
	\begin{itemize}
		\item бюджетното ограничение \eqref{eq:contrama}
		\item зададена начална стойност $ a(0)=a_0 $
		\item ограничението \eqref{eq:contramNoPonzi} върху заемите
		\item управлението $ c(t) $ се избира от $ [0,+\infty) $ за всяко $t$
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства -- необходими условия}
За да получим \emph{необходимите условия} от първи ред, образуваме хамилтониана
\begin{equation}
	J = u(c)e^{-(\rho-n)t} + \nu(t)[w(t)+(r(t)-n)a(t)-c(t)].
	\label{eq:contramHamilt}
\end{equation}

Тук променливата $ \nu(t) $ (наричана \textbf{спрегната}\index{спрегната променлива}, или множител на Лангранж) е \textbf{сенчестата (скритата) цена}\index{сенчеста (скрита) цена} на богатството $ a(t) $ 

Условията
\[ \frac{\partial J}{\partial c} = 0 \quad \text{ и }\quad \dot{\nu} = -\frac{\partial J}{\partial a} \]
имат в случая формата
\begin{equation}
	\nu (t) = u'(c(t))e^{-(\rho-n)t},
	\label{eq:contramNC1}
\end{equation}
\begin{equation}
	\dot{\nu}(t) = - (r(t)-n)\nu(t).
	\label{eq:contramNC2}
\end{equation}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства -- необходими условия}
	Към предходните условия трябва да добавим и \textbf{условието за трансверсалност}\index{условие за трансверсалност}:
	\begin{equation}
		\lim\limits_{t\rightarrow \infty}[\nu(t)a(t)] = 0.
		\label{eq:contramTransvcond}
	\end{equation} \bigskip
	
	Диференцираме \eqref{eq:contramNC1}, записано във вида \[ e^{(\rho-n)t}\nu(t) = u'(c(t)),  \]
	относно $ t $ и заменяме $ \dot{\nu} $ с израза от \eqref{eq:contramNC2}. Това ни дава
	\[ e^{(\rho-n)t}\nu(t)(\rho-r(t)) = u''(c(t))\dot{c}(t) . \]
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства -- необходими условия}
	Последният израз, комбиниран с \eqref{eq:contramNC1}, води до
	\begin{equation}
		r(t) = \rho - \left[\frac{u''(c(t))c(t)}{u'(c(t))}\right]\frac{\dot{c}(t)}{c(t)}.
		\label{eq:contramContEuler}
	\end{equation} 
	
	Понякога \eqref{eq:contramContEuler} се нарича \textbf{уравнение на Ойлер}.\bigskip
	
	Напомняме, че \textbf{еластичност} на дадена функция $f(x)$ се дефинира като $\frac{f'(x)x}{f(x)}$ (интерпретация!). \bigskip
	
	Съответно изразът в скобите в \eqref{eq:contramContEuler} е еластичността на $ u'(c) $ относно $ c $. Той е отрицателен, понеже $u''(c)<0$. \bigskip 
	
	\textbf{Интерпретация}: кога расте ( = се отлага във времето) потреблението?
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства -- необходими условия}
	От \eqref{eq:contramNC1} имаме
	\[ \nu(0)=u'(c(0))>0 \]
	и от \eqref{eq:contramNC2} намираме
	\begin{equation}
		\nu(t) = \nu(0)e^{ -\int_{0}^{t}(r(\tau)-n)\,d\tau }.
		\label{eq:contramNuSolution}
	\end{equation}
	
	Това позволява условието за трансверсалност \eqref{eq:contramTransvcond} да се запише във вида \begin{equation}
		\lim\limits_{t\rightarrow +\infty}a(t)e^{-\int_{0}^{t}(r(\tau)-n)\,d\tau} = 0,
		\label{eq:contramTransvcondnew}
	\end{equation}
	което е дори по-силно от желаното \eqref{eq:contramNoPonzi}. % N.B. Учебникът ни препраща към eq:optctrltransvcond, което вероятно е грешка
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства -- случаят на CRRA функция на полезност}
Една често използвана практика е да се вземе следната функция на полезност:
\begin{equation}
	u(c)=\frac{c^{1-\theta}-1}{1-\theta},~\theta = \const>0.
	\label{eq:contramCRRAfunc}
\end{equation}
Включването на $ (-1 ) $ в числителя е обичайно, защото така при $ \theta\rightarrow 1 $ се получава често използваната функция $ \ln c $. \bigskip

%Оттук нататък ще предполагаме, че $\theta \in (0, 1) $; за $\theta > 1 $ резултатите са аналогични.
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Домакинства -- случаят на CRRA функция на полезност}
При направения избор \eqref{eq:contramCRRAfunc} за $ u(c) $ равенство \eqref{eq:contramContEuler} има вида
\begin{equation}
	\frac{\dot{c}(t)}{c(t)} = \frac{1}{\theta}(r(t)-\rho).
	\label{eq:contramEulerCRRA}
\end{equation}

Оттук се получава
\begin{equation}
	c(t)=c(0)e^{ \frac{1}{\theta}[\bar{r}(t)-\rho]t},
	\label{eq:contramCsol}
\end{equation}
където $\bar{r}(t)$ е средният лихвен процент от $ 0 $ до $ t $:
\begin{equation}
	\bar{r}(t)=\frac{1}{t}\int_{0}^{t}r(\tau)\,d\tau .
	\label{eq:contramrbar}
\end{equation}

В частност, имаме $c(0)>0$ защото $ c(0)=0 $ би означавало $ c(t)=0 $ за всяко $ t $.
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Фирми}
		\begin{itemize}\itemsep1em
		\item Фирмите в модела произвеждат стоки, като заплащат за труда $ L(t) $ и за вложения капитал $ K(t) $
		\item Производствена технология:
		\begin{equation}
			Y = F(K,\hat{L}),
			\label{eq:contramProdfun}
		\end{equation}
		където $ \hat{L}=L(t)A(t) $ е в единици ефективен труд, а $ A(t) $ е нивото на технологията. 
		\item За улеснение приемаме, че $ A(t) $ расте с постоянен темп $ x\geq 0 $. Тогава, след нормирането $ A(0)=1 $, намираме \[ A(t)=e^{xt}. \]
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Фирми}
	\begin{itemize}\itemsep1em
		\item Променлива $z$ \emph{за единица ефективен труд} се записва като $\hat{z}$ и така нормираните $ Y $ и $ K $  са съответно
		\[ \hat{y}=\frac{Y}{\hat{L}} \quad \text{ и }\quad \hat{k}=\frac{K}{\hat{L}}, \]
		а производствената функция в интензивна форма е
		\[ \hat{y}=f(\hat{k})\equiv F(\hat{k},1), \]
		където $ f(0) = 0 $ и производните
		\begin{equation}
			\frac{\partial Y}{\partial K} = f'(\hat{k}),
			\label{eq:contramMPK}
		\end{equation}
		\begin{equation}
			\frac{\partial Y}{\partial L} = [f(\hat{k})-\hat{k}f'(\hat{k})]e^{xt}
			\label{eq:contramMPL}
		\end{equation}
		се предполагат положителни. 
	\end{itemize}
\end{frame}

\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Фирми}
	\begin{itemize}\itemsep1em
		\item От условията на Инада за $ F $ съответно следват
		\begin{equation}
			\lim\limits_{\hat{k}\rightarrow 0+} f'(\hat{k}) = \infty ,
			\label{eq:contramfprimeinfty}
		\end{equation} %
		\begin{equation}
			\lim\limits_{\hat{k}\rightarrow \infty} f'(\hat{k}) = 0.
			\label{eq:contramfprimezero}
		\end{equation}
	\end{itemize}
\end{frame}

\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Фирми}
	\begin{itemize}\itemsep1em
		\item Ще считаме, че домакинствата са притежатели на капитала, който фирмата наема при \textbf{рентна цена} $ R(t) $
		\item Ако капиталът се амортизира с постоянна норма $ \delta\geq 0 $, то чистата възвращаемост за домакинствата е $ R(t)-\delta $ на единица капитал
		\item Тъй като те могат да заемат средства на други домакинства при лихва $ r(t) $, вместо да купят капитал, по съображения за липса на арбитраж получаваме $ r(t)=R(t)-\delta $, т.е. $ R(t)=r(t) + \delta $
	\end{itemize}
\end{frame}

\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Фирми}
	\begin{itemize}\itemsep1em
		\item Представителната фирма във всеки момент от времето има \textbf{печалба} 
		\begin{equation}
			\Pi = F(K,\hat{L}) - (r+\delta)K - wL = \hat{L}[f(\hat{k})-(r+\delta)\hat{k}-w e^{-xt}].
			\label{eq:contramProfit}
		\end{equation}
		\item Тъй като $ \hat{L} $ се счита за дадено във всеки момент, фирмата максимизира печалбата си, подбирайки количеството капитал, и значи в сила е релацията
		\begin{equation}
			f'(\hat{k}(t)) = r(t)+\delta.
			\label{eq:contramProfitmax}
		\end{equation}
		\item Знакът на печалбата в оптимум ще зависи от стойността на $w$
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Фирми}
	\begin{itemize}\itemsep1em
		\item Приемаме, че фирмите функционират в условия на съвършена конкуренция, което означава, че заплатата ще бъде такава, че печалбата на една фирма да е нула. 
			\begin{itemize}\itemsep1em
			\item Ако имахме $\Pi > 0$ за една фирма, какъв би бил стимулът \`и да променя използваното количество труд (нагоре или надолу)? Какъв ефект върху заплатата очаквате, ако всички фирми в икономиката се държат така? Ако $\Pi < 0$?
			\end{itemize}
		\item Условието $ \Pi=0 $, т.е.
		\[ f(\hat{k})-(r+\delta)\hat{k}-w e^{-xt} = 0, \]
		заедно с \eqref{eq:contramProfitmax}, ни дава
		\begin{equation}
			[f(\hat{k})-\hat{k}f'(\hat{k})]e^{xt} = w.
			\label{eq:contramProfitmaxzero}
		\end{equation}
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Фирми}
	\begin{itemize}\itemsep1em
		\item При горната постановка можем да определим оптимално съотношение между производствените фактори, т.е. $ \hat{k} $
		\item Резултатите от това ще се използват в общия модел за определяне на обема на производството на ниво икономика
		\item Не можем обаче да определим производството на ниво отделна фирма
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Равновесие}
	\begin{itemize}\itemsep1em
		\item В разглежданата затворена икономика всяка единица капитал трябва да се притежава от някое домакинство
		\item Следователно активите трябва да са равни на капитала, $ a=k $
			\begin{itemize}\itemsep1em
			\item Ако икономиката беше \emph{отворена}, наличието на разлика между $ a $ и $ k $ би съответствало на нетните (!) активи на домакинствата по отношение на останалия свят
			\end{itemize} 
		\item Като поставим $ a=k=\hat{k}e^{xt} $ в уравнение \eqref{eq:contrama}, вземайки предвид, че $ c=\hat{c}e^{xt} $, както и формула \eqref{eq:contramProfitmaxzero} за $ w $ и условието \eqref{eq:contramProfitmax}, намираме
		\begin{equation}
			\dot{\hat{k}}=f(\hat{k})-\hat{c}-(x+n+\delta)\hat{k},
			\label{eq:contramkhat}
		\end{equation}
		където $ \hat{k}(0) $ е дадено
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Равновесие}
	\begin{itemize}\itemsep1em
		\item Уравнение \eqref{eq:contramkhat} само по себе си не е достатъчно за решаването на модела -- необходимо е да определим и $\hat{c}$
		\item Ще разгледаме частния случай на избора \eqref{eq:contramCRRAfunc} на функция на полезност
		\item Като вземем предвид, че $ \hat{c}=c e^{-xt} $ и $ r=f'(\hat{k})-\delta $ намираме чрез \eqref{eq:contramEulerCRRA} уравнението 
		\begin{equation}
			\frac{\dot{\hat{c}}}{\hat{c}}\equiv \frac{\dot{c}}{c}-x = \frac{1}{\theta}[f'(\hat{k})-\delta -\rho -\theta x].
			\label{eq:contramchat}
		\end{equation}
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Модел на Рамзи}
	\framesubtitle{Равновесие}
	\begin{itemize}\itemsep1em
		\item Системата \eqref{eq:contramkhat}-\eqref{eq:contramchat} за $ \hat{k},\hat{c} $ заедно с началното условие $ \hat{k}(0) $ и крайното условие \eqref{eq:contramTransvcondnew} (т.е. условие в $ +\infty $) ще определя оптималния път за $ \hat{k},\hat{c} $
		\item Предвид $ a=k=\hat{k}e^{xt} $ и уравнение \eqref{eq:contramProfitmax}, условието за трансверсалност \eqref{eq:contramTransvcondnew} приема вида
		\begin{equation}
			\lim\limits_{t \rightarrow \infty} \left\{ \hat{k}(t) \exp \left( -\int_{0}^{t}[f'(\hat{k}(\nu))-\delta-x-n]\, d\nu \right) \right\} =0.
			\label{eq:contramTransvcondnew1}
		\end{equation}
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Равновесни траектории и стационарни точки}
	\begin{itemize}\itemsep1em
		\item Подобно на модела на Солоу, \textbf{равновесна траектория} дефинираме като такава, върху която $ \hat{k} $ и $ \hat{c} $ имат \emph{постоянен темп на изменение}, т.е.
		\begin{equation}
			\frac{\dot{\hat{k}}}{\hat{k}}=\left(\gamma_{\hat{k}}\right)^*=\const,~\frac{\dot{\hat{c}}}{\hat{c}}=\left(\gamma_{\hat{c}}\right)^*=\const .
			\label{eq:contramSS}
		\end{equation}
		\item Може да се покаже, че върху равновесните траектории величините $ \hat{k} $ и $ \hat{c} $ са константи, т.е. представляват истинско стационарно състояние, а оттам същото се отнася и за $ \hat{y}=f(\hat{k}) $
		\item Следователно променливите $ k,c,y $ ,,на глава от работещите'' растат с постоянен темп $ x $, а тоталните променливи (за цялата икономика) $ K,C,Y $ -- с темп $ x+n $ 
		\item Това съответства точно на модела на Солоу
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Равновесни траектории и стационарни точки}
	\begin{itemize}\itemsep1em
		\item Определянето на стационарно състояние $ \hat{k}^*,\hat{c}^* $ става, като положим $ \dot{\hat{k}} $ и $ \dot{\hat{c}} $ равни на нула в \eqref{eq:contramkhat} и \eqref{eq:contramchat}
		\item Геометрично се интересуваме от двойките $ (\hat{k},\hat{c}) $, които удовлетворяват:
			\begin{enumerate}
			\item уравнението $ \dot{\hat{k}}=0 $, т.е.
			\begin{equation}
				\hat{c} = f(\hat{k}) - (x+n+\delta)\hat{k},
				\label{eq:contramdothatkzero}
			\end{equation}
			\item уравнението $ \dot{\hat{c}}=0 $, т.е.
			\begin{equation}
				f'(\hat{k}^*) = \delta + \rho + \theta x,
				\label{eq:contramdothatczero}
			\end{equation}
			което всъщност е вертикална права $ \hat{k}=\hat{k}^* $
		\end{enumerate}
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Стационарни точки на уравнения \eqref{eq:contramkhat} и \eqref{eq:contramchat}}
	\input{ckhatdotzero_edited.tex}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Равновесни траектории и стационарни точки}
	\begin{itemize}\itemsep1em
		\item На графиката е отчетено поведението на $ f(\hat{k}) $ и кривата е показана до стойността $ \hat{k}^{**} $, за която $ \hat{c}>0 $
		\item Условието за екстремум на тази крива е $ f'(\hat{k})-\delta = x+n $, което определя ,,златното правило'' за натрупване на капитал в модела на Солоу (с точност до условието $ x=0 $ в разгледания от нас вариант на модела на Солоу)
		\item На графиката стойността $ \hat{k}^* $ е взета вляво от $ \hat{k}_{\text{gold}}$ -- това е свойство на модела, когато е изпълнено условието за трансверсалност
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Динамика}
	\begin{itemize}\itemsep1em
		\item Фазовата диаграма на системата \eqref{eq:contramkhat}, \eqref{eq:contramchat} е от тип \textbf{седло} 
		\item Следователно съществува единствен устойчив път към стационарното състояние
	\end{itemize}
\end{frame}

\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Фазова диаграма на системата от уравнения \eqref{eq:contramkhat}-\eqref{eq:contramchat}}
	\input{ramseyphaseportrait_edited.tex}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Динамика}
	\begin{itemize}\itemsep1em
		\item Макар че на фазовата диаграма началното условие за системата е $\hat{k}_0, \hat{c}_0$, между двата компонента на началното условие има съществени разлики
			\begin{itemize}\itemsep1em
				\item Началният капитал на единица ефективен труд $\hat{k}_0$ е зададен 
				\item Стойността на $\hat{c}_0$ се избира като част от решението на оптимизационната задача на домакинството
			\end{itemize}
		\item Въпросът е кои точки $\hat{c}_0$ от вертикалната права през $\hat{k}_0$ удовлетворяват необходимите условия
		\item Може да се покаже, че единствената такава точка е тази, която е върху устойчивия път към стационарното състояние
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Динамика}
	\begin{itemize}\itemsep1em
		\item Устойчивата траектория за $\hat{k}_0 < \hat{k}^*$ е изобразена като изпъкнала функция на нашата фазова диаграма
		\item Дали това е така зависи от стойността на параметъра $\theta$:
			\begin{itemize}\itemsep1em
				\item За малки стойности на $\theta$ траекторията е изпъкнала (както е изобразена)
				\item За големи стойности на $\theta$ траекторията става вдлъбната и е близо до кривата $\dot{\hat{k}}=0$
			\end{itemize}
		\item Ако $\hat{k}_0 > \hat{k}^*$ се наблюдава обратната ситуация -- за малки стойности на $\theta$ траекторията е вдлъбната (виж диаграмата)
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Динамика}
	\begin{itemize}\itemsep1em
		\item В модела на Рамзи \[ \frac{C}{Y} = \frac{\hat{c}}{f(\hat{k})} \] и следователно нормата на спестяване е равна на \[ s=1-\frac{\hat{c}}{f(\hat{k})} \]
		\item Тази норма, за разлика от модела на Солоу, се изменя във времето и по принцип може да има различна динамика в зависимост от параметризацията на функцията на ползеност и производствената функция
		\item По-конкретни резултати могат да бъдат получени за случая на производствена функция на Коб-Дъглас $f(\hat{k})=A\hat{k}^\alpha$
	\end{itemize}
\end{frame}


\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Динамика (за случая Коб-Дъглас)}
	\begin{itemize}\itemsep1em
		\item Ако $f(\hat{k})=A\hat{k}^\alpha$, стационарната стойност на нормата на спестяване $ s^* = 1- \frac{\hat{c}^*}{f(\hat{k}^*)} $ е равна на величината \[ \frac{\alpha (x+n+\delta)}{\rho+\theta x + \delta} \]
		\item Може да се покаже, че $ s^*<\alpha $. Това изисква да е изпълнено $\rho > n + (1-\theta)x$, тъй като в противен случай ще бъде нарушено условието за трансверсалност.
		\item При означението \[ z=\frac{\hat{c}}{f(\hat{k})} \] е в сила \begin{equation}
			\gamma_z = \frac{\dot{z}}{z}=f'(\hat{k})\left[z(t)-\frac{\theta-1}{\theta}\right] + (\delta+\rho+\theta x)\left(s^* - \frac{1}{\theta}\right).
			\label{eq:contRP1eq9}
		\end{equation}
	\end{itemize}
\end{frame}

\begin{frame}[fragile]
	\frametitle{Резултати за модела на Рамзи}
	\framesubtitle{Динамика (за случая Коб-Дъглас)}
	\begin{itemize}\itemsep1em
		\item Ако $ s^* > \frac{1}{\theta} $, то $s$ нараства с нарастването на $\hat{k}$
		\item Ако $ s^* = \frac{1}{\theta} $, то $s(t)$ е константа $(=s^*)$
		\item Ако $ s^* < \frac{1}{\theta} $, то $s$ е намаляваща функция от $\hat{k}$
	\end{itemize}
\end{frame}


\end{document}





%\newcounter{slidenum} 
%\setcounter{slidenum}{1}
%\begin{frame}[fragile]\stepcounter{slidenum}
%	\frametitle{ (\arabic{slidenum})}
%
%\end{frame}

